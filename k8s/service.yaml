apiVersion: v1
kind: Service
metadata:
  name: images
  labels:
    app: images
spec:
  ports:
  - name:
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: images
  sessionAffinity: None
  type: NodePort
status:
  loadBalancer: {}